import React from "react";
import BoardBottom from "./";

export default {
  title: "Organisms/BoardBottom",
  component: BoardBottom,
};

const Template = (args) => <BoardBottom {...args} />;

export const Control = Template.bind({});
