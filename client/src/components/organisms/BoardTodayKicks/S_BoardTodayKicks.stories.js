import React from "react";

import S_BoardTodayKicks from "./S_BoardTodayKicks";

export default {
  title: "Skeleton/S_BoardTodayKicks",
  component: S_BoardTodayKicks,
};

const Template = (args) => <S_BoardTodayKicks {...args} />;

export const Control = Template.bind({});
