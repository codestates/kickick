import React from "react";
import BoardListTitle from "./BoardListTitle";

export default {
  title: "Atoms/BoardList/BoardListTitle",
  component: BoardListTitle,
};

const Template = (args) => <BoardListTitle {...args} />;

export const Control = Template.bind({});
