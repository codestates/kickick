import BoardTodayKicks from "./";

export default {
  title: "Atoms/BoardTodayKicks",
  component: BoardTodayKicks,
};

const Template = (args) => <BoardTodayKicks {...args} />;

export const Control = Template.bind({});
