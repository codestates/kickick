import React, { useEffect, useState } from "react";
import styled from "styled-components";

import EditKickBoard from "./EditKickBoard";

export default function Write() {
  return <Container></Container>;
}

const Container = styled.div`
  display: flex;
  padding: 3rem 1rem;
`;
